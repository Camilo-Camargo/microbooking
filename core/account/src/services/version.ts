import { VersionReq, VersionReq__Output } from "../types/account/VersionReq";
import { VersionRes } from "../types/account/VersionRes";

export async function version(req: VersionReq__Output): Promise<VersionRes>{
  return { 
  }
}

