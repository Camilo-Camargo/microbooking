import { VersionReq } from "../types/account/VersionReq";
import { VersionRes } from "../types/account/VersionRes";

export async function version(req: VersionReq): Promise<VersionRes>{
  return { 
  }
}

